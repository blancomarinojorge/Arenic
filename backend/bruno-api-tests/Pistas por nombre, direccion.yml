info:
  name: Pistas por nombre, direccion
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/api/clubs/search?query=real
  params:
    - name: query
      value: real
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
